NOTIFICATION_PRIORITY_MAP_CSS = {
    "debug": "info",
    "low": "info",
    "normal": "info",
    "high": "warning",
    "urent": "danger",
}

# These extensions are ignored for logging and 'touching' auth items for last updated.
IGNORED_EXTENSIONS = (".png", ".js", ".css", ".jpg", ".jpeg", ".gif", ".ico", ".woff2", ".map",
                      "site.webmanifest")
